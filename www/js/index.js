var app = {
    // Construtor da aplicação
    initialize: function() {
        document.addEventListener('deviceready', this.onDeviceReady.bind(this), false);
    },

    // Manipulador de eventos deviceready	
    //
    // Ligue qualquer evento cordova aqui. Eventos comuns são:
    // 'pause', 'resume', etc.
    onDeviceReady: function() {
        
		

    // Atualização de um evento recebido no DOM
    receivedEvent: function() {
        
		
		
    }
};

app.initialize();